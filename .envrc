# direnv: auto-load the pinned BSC Lab dev shell on `cd` into the repo.
# Requires direnv + nix. Run `direnv allow` once to enable.
# Without direnv, use `nix develop` manually (or the plain npm/make workflow).
use flake
