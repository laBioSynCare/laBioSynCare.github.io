# SSTIM ontology — frozen snapshot 0.16.0

These files are a **byte-identical, immutable copy** of the SSTIM ontology as of
version `0.16.0`. They exist so that
`owl:versionIRI <https://w3id.org/sstim/0.16.0>` resolves to a frozen
artifact that can be cited without ambiguity, even after the top-level
`/ontology/*.ttl` files continue to evolve toward the next version.

`sstim-namespace.ttl`, `sstim-exposure-namespace.ttl` are
**generated namespace catalogues**:
the concatenation of the frozen modules sharing each hash namespace, in manifest
order. `sstim-namespace.ttl` is what
`https://w3id.org/sstim/0.16.0` resolves to, because `sstim-core.ttl` is
the two-class Kernel rather than the whole ontology (ADR 0043). Every other file
here is a byte-identical copy of its top-level source.

Profile entrypoints were release-prepared before this copy: every
`owl:imports` and PROF artifact target is an exact versioned URL.
`manifest.json` advertises the immutable URL of every snapshotted artifact,
and its `$schema` points at the frozen `manifest.schema.json` sibling.

- Source commit: `388352ff2d46c6ea26564809a485c2b13985ab4b`
- Generated by: `scripts/snapshot-ontology.mjs`
- Do not hand-edit. To correct an unpublished snapshot, re-run the script with
  `--force`. To cut a new version, bump `owl:versionInfo` /
  `owl:versionIRI` in `sstim-core.ttl` first, then snapshot the new number.

Files: `sstim-core.ttl`, `sstim-stimulus.ttl`, `sstim-core-shapes.ttl`, `sstim-common.ttl`, `sstim-technique.ttl`, `sstim-configuration.ttl`, `sstim-session.ttl`, `sstim-evidence.ttl`, `sstim-neuromodulation.ttl`, `sstim-patch-studio.ttl`, `sstim-vocab.ttl`, `sstim-exposure.ttl`, `sstim-ecosystem.ttl`, `sstim-neuromodulation-evidence.ttl`, `sstim-evidence-exposure.ttl`, `sstim-technique-exposure.ttl`, `sstim-alignments.ttl`, `sstim-shapes.ttl`, `sstim-kernel-profile.ttl`, `sstim-core-profile.ttl`, `sstim-core-plus-profile.ttl`, `sstim-full-profile.ttl`, `sstim-namespace.ttl`, `sstim-exposure-namespace.ttl`, `manifest.json`, `manifest.schema.json`.
