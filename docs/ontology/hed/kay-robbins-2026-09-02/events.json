{
  "onset": {
    "Description": "Seconds from the session clock origin. The clock is the audio hardware clock the stimulus was rendered against, not a host wall clock, so offsets are comparable to the delivered signal rather than to a system time."
  },
  "duration": {
    "Description": "Always n/a in this file. Every row is an instantaneous mark on the session timeline. Spans are expressed by paired scope tags (a delivery span runs from an Onset to the next Offset), so a duration here would be a second, redundant, and possibly disagreeing account of the same span."
  },
  "event_id": {
    "LongName": "SSTIM session event type",
    "Description": "The kind of occurrence the delivering system observed, given as the skos:notation of a concept in the SSTIM Session Event Type vocabulary. Categorical, so a value repeats whenever the same kind of occurrence happens again. Each level's TermURL is the shared, resolvable SSTIM definition of that event type; that IRI, not this file, is where the meaning lives.",
    "TermURL": "https://w3id.org/sstim/vocab#SessionEventTypeScheme",
    "Levels": {
      "session-open": {
        "Description": "The session instance was opened and its clock origin captured. Always the first row, at offset zero. Opens the session scope; nothing is being delivered yet.",
        "TermURL": "https://w3id.org/sstim/vocab#eventSessionOpen"
      },
      "playback-start": {
        "Description": "Stimulus delivery began. Opens the delivery scope and states the beat frequency the stimulus carries from this point.",
        "TermURL": "https://w3id.org/sstim/vocab#eventPlaybackStart"
      },
      "playback-pause": {
        "Description": "Stimulus delivery stopped without ending the session. Elapsed time continues to run, delivered time does not. Closes the delivery scope, so the pause is the span between this row and the next playback-resume.",
        "TermURL": "https://w3id.org/sstim/vocab#eventPlaybackPause"
      },
      "playback-resume": {
        "Description": "Stimulus delivery restarted after a pause, with the session scope never having closed. Opens a new delivery scope and restates the beat frequency.",
        "TermURL": "https://w3id.org/sstim/vocab#eventPlaybackResume"
      },
      "playback-stop": {
        "Description": "Stimulus delivery ended. Closes the delivery scope for the last time, before the session itself closes.",
        "TermURL": "https://w3id.org/sstim/vocab#eventPlaybackStop"
      },
      "parameter-changed": {
        "Description": "A delivery quantity was set to a new value part way through the session, here by the participant rather than by the protocol. Carries the parameter and the value it took, so a stimulus that steps mid session stays readable from the timeline instead of only from the specification.",
        "TermURL": "https://w3id.org/sstim/vocab#eventParameterChanged"
      },
      "safety-limit-applied": {
        "Description": "A delivery parameter was constrained by a safety boundary, so the value actually delivered differs from the value requested one row earlier. Without this row the record would describe a stimulus that was never delivered.",
        "TermURL": "https://w3id.org/sstim/vocab#eventSafetyLimitApplied"
      },
      "engine-fallback": {
        "Description": "Delivery moved to a different engine implementation mid session, for example because a required capability was unavailable. Recorded because the stimulus after this point was generated by different code, which is material to reproducibility.",
        "TermURL": "https://w3id.org/sstim/vocab#eventEngineFallback"
      },
      "observation-collected": {
        "Description": "A self report was collected from the participant at this point on the timeline. Places the act of asking, which is separate from what was answered; the answers live in the SSTIM session record, not here.",
        "TermURL": "https://w3id.org/sstim/vocab#eventObservationCollected"
      },
      "session-complete": {
        "Description": "The session delivered its intended duration and closed normally. Closes the session scope.",
        "TermURL": "https://w3id.org/sstim/vocab#eventSessionComplete"
      }
    },
    "HED": {
      "session-open": "(Def/Sstim-session, Onset)",
      "playback-start": "(Def/Sstim-delivery, Onset, {mod_frequency})",
      "playback-pause": "(Def/Sstim-delivery, Offset), (Agent-action, (Experiment-participant, (Press, (Mouse-button, Pause))))",
      "playback-resume": "(Def/Sstim-delivery, Onset, ({mod_frequency}, (Agent-action, (Experiment-participant, (Press, (Mouse-button, Label/Resume))))))",
      "playback-stop": "(Def/Sstim-delivery, Offset)",
      "parameter-changed": "(Experiment-control, (Controller-agent, (Modify, ({level}))))",
      "safety-limit-applied": "(Experiment-control, (Controller-agent, (Modify, (Constrained, ({level})))))",
      "engine-fallback": "(Experiment-control, (Controller-agent, (Modify, (Label/Engine, (Performed-using, (Computational-evidence))))))",
      "observation-collected": "(Experiment-procedure, (Experiment-participant, (Perform/Report)))",
      "session-complete": "(Def/Sstim-session, Offset)"
    }
  },
  "mod_frequency": {
    "LongName": "Binaural beat frequency",
    "Description": "The difference frequency between the two carriers, in hertz, which is the rate the session is configured around. Present on the rows that open a delivery scope and n/a elsewhere, because those are the rows where a rate begins to apply. Spliced into the event_id annotation rather than annotated on its own, so the rate is stated inside the delivery scope it belongs to.",
    "Units": "Hz",
    "TermURL": "https://w3id.org/sstim/exposure#hasBeatFrequencyHz",
    "HED": "(Sensory-event, Experimental-stimulus, Auditory-presentation, Frequency/# Hz)"
  },
  "level": {
    "LongName": "Delivery level",
    "Description": "Delivery level as a normalised amplitude in the closed interval 0 to 1, present only on the rows where the level changed or was constrained. On a parameter-changed row this is the value the participant requested; on a safety-limit-applied row it is the value actually delivered after the boundary was applied. Spliced into the event_id annotation, so it reads as part of the control action rather than as a standalone measurement.",
    "TermURL": "https://w3id.org/sstim/vocab#paramLevel",
    "HED": "(Parameter-label/Level, Parameter-value/#)"
  },
  "sstim_hed_definitions": {
    "Description": "HED definitions the event_id annotations reference. Not a column of events.tsv. Onset and Offset are scope tags that HED requires to be paired with exactly one Def/, so the events do not validate without these in scope.",
    "HED": {
      "Sstim-session": "(Definition/Sstim-session, (Experiment-structure, Time-block))",
      "Sstim-delivery": "(Definition/Sstim-delivery)"
    }
  },
  "SstimStimulusIdentity": {
    "Description": "What was delivered, identified once for the whole file rather than repeated per row, because it does not change: a binaural beat, as defined at https://w3id.org/sstim/vocab#techBinauralBeats, rendered by the mechanism at https://w3id.org/sstim/vocab#mechanismBinauralBeat. Those IRIs are the shared definitions; they say what a binaural beat is, which is the thing a second reader has to agree with us about before the numbers in this file mean anything. The link travels in TermURL because no HED 8.4.0 tag can carry an SSTIM IRI: the scheme fails on its double slash (TAG_INVALID), percent encoding fails on the per cent sign (CHARACTER_INVALID), and the fragment separator is HED's own placeholder character, so a hash IRI in a Levels annotation is rejected as PLACEHOLDER_INVALID and in a Definition body it silently breaks every Def that references it. SSTIM uses hash IRIs throughout, which is exactly the form that cannot be carried. TermURL is also where a BIDS sidecar already expects the link, per column and per level.",
    "TermURL": "https://w3id.org/sstim/vocab#techBinauralBeats"
  },
  "SstimPauseConvention": {
    "Description": "A pause is expressed as an Offset on the delivery scope followed by an Onset that reopens it, not with the HED Pause tag. In this file the pause runs from 420.000 s to 480.000 s. Pause and Inset were used in an earlier draft and are wrong for this: that tag was designed for suspending a recording rather than a stimulus, and it is not what current HED practice uses. The cost is that HED alone cannot tell a pause from a stop and a restart; SSTIM keeps that distinction in the event type, which is why the native event_id column is the one to read."
  },
  "SstimGeneratedAnnotationNote": {
    "Description": "HED here is generated from the SSTIM session record by a versioned one way crosswalk, and is never primary storage. SSTIM is canonical, HED is derived, so where the two disagree the native columns win. The mapping asserts no concept equivalence between an SSTIM event type and a HED tag; it is a profile mapping. Every string in this file validates against HED 8.4.0 with hedtools, with the definitions above in scope."
  },
  "SstimDeclaredLossNote": {
    "Description": "What a HED only consumer cannot recover from this file. First, playback-pause and playback-stop emit identical HED, and so do playback-resume and playback-start, so the pause boundary is invisible in HED. Second, session-complete and session-interrupt would emit identical HED, because HED 8.4.0 has no Incomplete, Abort or Terminate tag, so a completed session cannot be told from an abandoned one. Third, safety-limit-applied says a parameter was constrained but not that the constraint was protective rather than an ordinary adjustment, and the requested value stays in the SSTIM record. Fourth, engine-fallback records that delivery moved to different code but not which engines were involved. These losses are declared rather than papered over, and each is tested in both directions."
  },
  "SstimScopeNote": {
    "Description": "Synthetic demonstrator data describing no one. It illustrates how an SSTIM session record projects into a BIDS style events file with HED annotation, and is not evidence about any effect of any stimulus. This is a BIDS style pair, not a BIDS dataset: no dataset_description.json, no entity naming, and no validator clean directory around it."
  }
}
