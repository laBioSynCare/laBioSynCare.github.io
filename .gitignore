.DS_Store
.env
.env.*
!.env.example

sandbox/
node_modules/
.svelte-kit/
dist/

# Nix / direnv (flake.nix + flake.lock are committed; build outputs are not)
.direnv/
result
result-*

docs/credentials/
